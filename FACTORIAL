#include <stdio.h>
void main()
{
   int a,factorial=1;
   printf("Enter a number: ");
   scanf("%d",&a);
   for (int i=1;i<=a;i++)
   {
       factorial*=i;
      
   }
   printf("Factorial of %d is %d:",a,factorial);
}
